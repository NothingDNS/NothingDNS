{{- if .Values.monitoring.serviceMonitor.enabled }}
{{- if not .Values.config.metrics.enabled }}
{{- fail "monitoring.serviceMonitor.enabled requires config.metrics.enabled=true" }}
{{- end }}
{{- if and .Values.monitoring.serviceMonitor.namespace (ne .Values.monitoring.serviceMonitor.namespace .Release.Namespace) }}
{{- fail "monitoring.serviceMonitor.namespace must match the release namespace because scrape credentials are namespace-scoped" }}
{{- end }}
{{- $secretName := default (include "nothingdns.fullname" .) .Values.auth.existingSecret }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "nothingdns.fullname" . }}
  {{- if .Values.monitoring.serviceMonitor.namespace }}
  namespace: {{ .Values.monitoring.serviceMonitor.namespace }}
  {{- else }}
  namespace: {{ .Release.Namespace }}
  {{- end }}
  labels:
    {{- include "nothingdns.labels" . | nindent 4 }}
    {{- with .Values.monitoring.serviceMonitor.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  endpoints:
    - port: metrics
      interval: {{ .Values.monitoring.serviceMonitor.interval }}
      scrapeTimeout: {{ .Values.monitoring.serviceMonitor.scrapeTimeout }}
      path: {{ .Values.config.metrics.path }}
      authorization:
        type: Bearer
        credentials:
          name: {{ $secretName }}
          key: metrics-auth-token
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  selector:
    matchLabels:
      {{- include "nothingdns.selectorLabels" . | nindent 6 }}
{{- end }}
